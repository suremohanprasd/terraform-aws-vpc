# VPC Module
This Module is developed for joindevops company, Projects inside joindevops follos this module to create VPC.

## Inputs
* project (required) - Users should specify their project name
* environment (required) - Users should specify their environments
* cidr_block (optional) - users can provide their CIDR block, default is 10.0.0.0/16